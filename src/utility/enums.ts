export enum Status {
    ACTIVE = 'active',
    DPRC = 'dprc'
}

export enum ActionStatus {
    SENT,
    PROCESSING,
    FAILED,
    COMPLETED,
}

export enum TokenType {
    NONE='-',
    CAT='cat',
    PET='pet',
}